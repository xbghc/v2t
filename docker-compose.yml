services:
  v2t:
    build: .
    image: v2t:latest
    volumes:
      # 挂载输出目录（下载的视频和生成的文件会保存在这里）
      - ./output:/output
      # 挂载配置文件（首次使用需要先创建 config.json）
      - ./config.json:/root/.config/v2t/config.json:ro
    # 使用示例: docker compose run --rm v2t "视频链接" --summary
